apiVersion: v1
kind: Service
metadata:
  name: fintech-microservice-service
spec:
  type: NodePort
  selector:
    app: fintech-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30081